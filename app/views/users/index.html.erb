<%= render 'shared/content_title', title: 'Manage All Users' %>

<ul>
  <% @users.each do |user| %>
    <li><strong>Username:</strong> <%= user.username %> | 
        <strong>Role:</strong> <%= user.role %> |
        <%= link_to 'edit', edit_user_path(user) %> 
  <% end %>
</ul>

<h5>Register new user:</h5>
<%= render 'form' %>